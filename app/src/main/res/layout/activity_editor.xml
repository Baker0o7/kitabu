<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/editorRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_primary">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/transparent"
        app:navigationIcon="@drawable/ic_back"
        app:navigationIconTint="@color/text_primary"/>

    <!-- Markdown toolbar -->
    <HorizontalScrollView android:layout_width="match_parent" android:layout_height="44dp"
        android:scrollbars="none" android:background="@color/bg_elevated"
        android:paddingHorizontal="8dp">
        <LinearLayout android:id="@+id/markdownToolbar"
            android:layout_width="wrap_content" android:layout_height="match_parent"
            android:orientation="horizontal" android:gravity="center_vertical"/>
    </HorizontalScrollView>

    <!-- Color + tag row -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="vertical" android:paddingHorizontal="16dp" android:paddingTop="4dp">

        <TextView android:id="@+id/colorPickerToggle"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="🎨 Color" android:textColor="@color/text_secondary" android:textSize="12sp"
            android:paddingVertical="6dp"/>

        <LinearLayout android:id="@+id/colorPickerPanel"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="horizontal" android:paddingBottom="8dp" android:visibility="gone">
            <View android:id="@+id/color0" android:layout_width="28dp" android:layout_height="28dp" android:layout_margin="5dp" android:background="@drawable/bg_color_chip"/>
            <View android:id="@+id/color1" android:layout_width="28dp" android:layout_height="28dp" android:layout_margin="5dp" android:background="@drawable/bg_color_chip"/>
            <View android:id="@+id/color2" android:layout_width="28dp" android:layout_height="28dp" android:layout_margin="5dp" android:background="@drawable/bg_color_chip"/>
            <View android:id="@+id/color3" android:layout_width="28dp" android:layout_height="28dp" android:layout_margin="5dp" android:background="@drawable/bg_color_chip"/>
            <View android:id="@+id/color4" android:layout_width="28dp" android:layout_height="28dp" android:layout_margin="5dp" android:background="@drawable/bg_color_chip"/>
            <View android:id="@+id/color5" android:layout_width="28dp" android:layout_height="28dp" android:layout_margin="5dp" android:background="@drawable/bg_color_chip"/>
        </LinearLayout>

        <!-- Tag chips -->
        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chipGroupTags"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="4dp"/>

        <LinearLayout
            android:id="@+id/backlinksContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:visibility="gone"/>
    </LinearLayout>

    <!-- Backlinks bar -->
    <TextView android:id="@+id/tvBacklinks"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:paddingHorizontal="20dp" android:paddingVertical="4dp"
        android:textColor="@color/accent" android:textSize="12sp" android:visibility="gone"/>

    <!-- Title -->
    <EditText android:id="@+id/etTitle"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:paddingHorizontal="20dp" android:paddingVertical="8dp"
        android:hint="Title" android:textColorHint="@color/text_hint" android:textColor="@color/text_primary"
        android:textSize="22sp" android:fontFamily="sans-serif-medium"
        android:background="@android:color/transparent" android:inputType="textCapSentences|textMultiLine" android:maxLines="3"/>

    <View android:layout_width="match_parent" android:layout_height="1dp"
        android:layout_marginHorizontal="20dp" android:background="@color/divider"/>

    <!-- Preview overlay -->
    <ScrollView android:id="@+id/scrollPreview"
        android:layout_width="match_parent" android:layout_height="0dp" android:layout_weight="1"
        android:visibility="gone">
        <TextView android:id="@+id/tvPreview"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:paddingHorizontal="20dp" android:paddingVertical="12dp"
            android:textColor="@color/text_primary" android:textSize="16sp"
            android:lineSpacingMultiplier="1.6"/>
    </ScrollView>

    <!-- Content editor -->
    <EditText android:id="@+id/etContent"
        android:layout_width="match_parent" android:layout_height="0dp" android:layout_weight="1"
        android:paddingHorizontal="20dp" android:paddingTop="12dp" android:paddingBottom="32dp"
        android:hint="Start writing… (Markdown supported)" android:textColorHint="@color/text_hint"
        android:textColor="@color/text_primary" android:textSize="16sp" android:lineSpacingMultiplier="1.5"
        android:fontFamily="sans-serif" android:background="@android:color/transparent"
        android:inputType="textCapSentences|textMultiLine" android:gravity="top"/>

</LinearLayout>
